#!/usr/bin/env bash
# Kills the 7-highlander process with the SIGKILL signal.

# This version uses pgrep with a pattern to avoid finding itself AND passes shellcheck.
kill -9 "$(pgrep -f '7-[h]ighlander')"
